/**
 * A stack data structure that supports all standard stack operations and an additional operation getMin()
 * which returns the minimum element from the stack in O(1) time.
 *
 * The SpecialStack uses only standard stack operations and maintains O(1) time and space complexity
 * for all operations.
 */
export class SpecialStack {
	/**
	 * Pushes an element onto the stack.
	 *
	 * @param {number} value - The value to be pushed onto the stack.
	 * @returns {void}
	 */
	push(value) {
		// Implementation goes here
	}

	/**
	 * Removes the top element from the stack.
	 *
	 * @returns {number} - The removed element.
	 */
	pop() {
		// Implementation goes here
	}

	/**
	 * Returns the top element of the stack without removing it.
	 *
	 * @returns {number} - The top element of the stack.
	 */
	top() {
		// Implementation goes here
	}

	/**
	 * Checks if the stack is empty.
	 *
	 * @returns {boolean} - Returns true if the stack is empty, otherwise false.
	 */
	isEmpty() {
		// Implementation goes here
	}

	/**
	 * Checks if the stack is full.
	 *
	 * @returns {boolean} - Returns true if the stack is full, otherwise false.
	 */
	isFull() {
		// Implementation goes here
	}

	/**
	 * Returns the minimum element in the stack.
	 *
	 * @returns {number} - The minimum element in the stack.
	 */
	getMin() {
		// Implementation goes here
	}
}

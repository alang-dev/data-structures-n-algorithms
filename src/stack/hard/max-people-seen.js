/**
 * Finds the maximum number of people a person can see while standing in a line in both directions.
 *
 * @param {number[]} height - An array where `height[i]` represents the height of the i-th person standing in a line.
 * @returns {number} The maximum number of people that a person can see, including themselves.
 *
 * @example
 * // Input: heights = [6, 2, 5, 4, 5, 1, 6]
 * // Output: 6
 * // Explanation: Person 1 can see five other people plus themselves, making a total of 6.
 *
 * @example
 * // Input: heights = [1, 3, 6, 4]
 * // Output: 3
 * // Explanation: Person 3 can see two other people plus themselves, making a total of 3.
 */
export function maxPeopleSeen(height) {
	// Implementation to be provided by the user
}

/**
 * Sorts a stack using recursion.
 *
 * The function sorts a stack such that the largest element is at the top and the smallest element is at the bottom.
 * It uses recursion to sort the stack. The main idea is to remove all elements from the stack, sort them, and then
 * insert them back into the stack in sorted order.
 *
 * @param {number[]} stack - An array representing the stack where the top of the stack is at the end of the array.
 *
 * @example
 * const stack = [-3, 14, 18, -5, 30];
 * sortStack(stack);
 * console.log(stack);
 * // Outputs: [30, 18, 14, -3, -5]
 * // Explanation: The stack is sorted in descending order: 30 > 18 > 14 > -3 > -5
 *
 * @example
 * const stack = [1, 2, 3];
 * sortStack(stack);
 * console.log(stack);
 * // Outputs: [3, 2, 1]
 * // Explanation: The stack is sorted in descending order: 3 > 2 > 1
 */
export function sortStack(stack) {
	// Implementation goes here
}

/**
 * @file infix-to-prefix.js
 * @description Converts an infix expression to prefix form.
 *
 * An infix expression has operators placed between their operands, like "A * B + C / D".
 * A prefix expression has operators placed before their operands, like "+ * A B / C D".
 * This function converts an infix expression directly to its equivalent prefix form.
 *
 * @param {string} expression - The infix expression to be converted.
 * @returns {string} - The prefix expression.
 *
 * @example
 * // Example 1
 * // Input: "A * B + C / D"
 * // Output: "+ * A B / C D"
 * const result1 = infixToPrefix("A * B + C / D");
 *
 * @example
 * // Example 2
 * // Input: "(A - B / C) * (A / K - L)"
 * // Output: "* - A / B C - / A K L"
 * const result2 = infixToPrefix("(A - B / C) * (A / K - L)");
 */
export function infixToPrefix(expression) {
	// Implementation goes here
}

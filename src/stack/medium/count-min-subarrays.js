/**
 * Counts the number of subarrays where the first element is not greater than any other element in the subarray.
 *
 * @param {number[]} arr - The input array of integers.
 * @returns {number} The count of subarrays where the first element is the minimum.
 *
 * @example
 * const result = countMinSubarrays([1, 2, 1]);
 * // Output: 5
 * // Explanation: The valid subarrays are: {1}, {1, 2}, {1, 2, 1}, {2}, {1}.
 *
 * @example
 * const result = countMinSubarrays([1, 3, 5, 2]);
 * // Output: 8
 * // Explanation: The valid subarrays are: {1}, {1, 3}, {1, 3, 5}, {1, 3, 5, 2}, {3}, {3, 5}, {5}, {2}.
 */
export function countMinSubarrays(arr) {
	// Implementation goes here
}

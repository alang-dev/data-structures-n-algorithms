/**
 * Finds the lexicographically largest subsequence containing all distinct characters only once.
 *
 * @param {string} s - The input string.
 * @returns {string} The lexicographically largest subsequence with all distinct characters.
 *
 * @example
 * const result = largestDistinctSubsequence('ababc');
 * // Output: 'bac'
 * // Explanation: The subsequences with all distinct characters are {'abc', 'bac'}. The lexicographically largest is 'bac'.
 *
 * @example
 * const result = largestDistinctSubsequence('zydsbacab');
 * // Output: 'zydscab'
 * // Explanation: The largest subsequence with all distinct characters is 'zydscab'.
 */
export function largestDistinctSubsequence(s) {
	// Implementation goes here
}

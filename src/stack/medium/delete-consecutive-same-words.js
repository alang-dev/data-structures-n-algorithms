/**
 * Deletes consecutive same words in a sequence.
 *
 * @param {string[]} words - An array of words in the sequence.
 * @returns {number} The number of words left after removing consecutive duplicates.
 *
 * @example
 * const result = deleteConsecutiveSameWords(['ab', 'aa', 'aa', 'bcd', 'ab']);
 * // Output: 3
 * // Explanation: The consecutive 'aa' are destroyed, resulting in the sequence 'ab', 'bcd', 'ab'.
 *
 * const result = deleteConsecutiveSameWords(['tom', 'jerry', 'jerry', 'tom']);
 * // Output: 0
 * // Explanation: Both 'jerry' are destroyed first, then both 'tom' are destroyed, leaving no words.
 */
export function deleteConsecutiveSameWords(words) {
	// Implementation goes here
}

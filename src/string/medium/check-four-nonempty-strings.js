/**
 * Check if a string can be split into exactly four distinct non-empty substrings.
 *
 * @param {string} str - The input string to check.
 * @returns {string} - "Yes" if such a split is possible, otherwise "No".
 *
 * @example
 * // Example 1
 * // Input: "geeksforgeeks"
 * // Output: "Yes"
 * // Explanation: "geeks", "for", "gee", "ks" are four distinct non-empty substrings.
 *
 * @example
 * // Example 2
 * // Input: "aaabb"
 * // Output: "No"
 * // Explanation: It's not possible to split into four distinct non-empty substrings.
 */
function canSplitIntoFourStrings(str) {
	// Function implementation
}

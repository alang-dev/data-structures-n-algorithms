/**
 * Replace all spaces in a string with '%20'. Assumes the string has sufficient space at the end to hold the additional characters.
 *
 * @param {string} str - The input string to replace spaces in.
 * @param {number} trueLength - The true length of the string (excluding trailing spaces).
 * @returns {string} - The modified string with spaces replaced by '%20'.
 *
 * @example
 * // Example 1
 * // Input: "Mr John Smith", 13
 * // Output: "Mr%20John%20Smith"
 *
 * @example
 * // Example 2
 * // Input: "Mr John Smith   ", 13
 * // Output: "Mr%20John%20Smith"
 */
export function urlify(str, trueLength) {
	// function implementation here
}

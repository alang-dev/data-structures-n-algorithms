/**
 * Count total substrings of a string that are anagrams to each other.
 *
 * @param {string} str - The input string consisting of lowercase alphabet characters.
 * @returns {number} - The total count of substrings that are anagrams to each other.
 *
 * @example
 * // Example 1
 * // Input: "xyyx"
 * // Output: 4
 * // Explanation: Total substrings which are anagram to each other: {"x", "x"}, {"y", "y"}, {"xy", "yx"}, {"xyy", "yyx"}
 *
 * @example
 * // Example 2
 * // Input: "geeg"
 * // Output: 4
 * // Explanation: Total substrings which are anagram to each other: {"g", "g"}, {"e", "e"}, {"ge", "eg"}, {"geg", "geg"}
 */
function countAnagramSubstrings(str) {
	// function implementation here
}

/**
 * Given two binary trees, check if each of their levels is an anagram of the other.
 *
 * @param {TreeNode} root1 - The root node of the first binary tree.
 * @param {TreeNode} root2 - The root node of the second binary tree.
 * @returns {boolean} - True if each level of both trees is an anagram of the other, otherwise false.
 *
 * @example
 * // Example
 * // Tree 1:
 * // Level 0 : 1
 * // Level 1 : 3, 2
 * // Level 2 : 5, 4
 * //
 * // Tree 2:
 * // Level 0 : 1
 * // Level 1 : 2, 3
 * // Level 2 : 4, 5
 * //
 * // Output: true
 */
function areLevelsAnagram(root1, root2) {
	// function implementation here
}

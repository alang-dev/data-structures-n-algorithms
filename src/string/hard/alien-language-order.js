/**
 * Finds the order of characters in an alien language based on a sorted dictionary of words.
 *
 * @param {string[]} words - Array of words sorted in the alien language.
 * @returns {string} - String representing the order of characters in the language.
 *
 * @example
 * // Example 1:
 * // Input: ["baa", "abcd", "abca", "cab", "cad"]
 * // Output: "bdac"
 * // Explanation: Characters order based on the given dictionary of words.
 *
 * // Example 2:
 * // Input: ["caa", "aaa", "aab"]
 * // Output: "cab"
 * // Explanation: Characters order based on the given dictionary of words.
 */
function alienLanguageOrder(words) {
	// Function implementation will go here
}
